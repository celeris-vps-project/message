package com.celeris.message.config;

import org.springframework.context.annotation.Configuration;

/**
 * Mail configuration.
 * Spring Boot auto-configures JavaMailSender based on spring.mail.* properties.
 * Add custom mail configuration here if needed (e.g., custom session properties).
 */
@Configuration
public class MailConfig {
    // Auto-configured by Spring Boot Mail starter
}
